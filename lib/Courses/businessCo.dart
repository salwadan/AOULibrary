import 'package:flutter/material.dart';

class BusinessCo extends StatelessWidget {
  const BusinessCo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}